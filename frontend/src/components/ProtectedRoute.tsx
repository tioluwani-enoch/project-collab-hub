export default function ProtectedRoute() {
  return (
    <>
      <p>This page is a protected route for the the other secured pages, so only people who have signed up to the websote can use it</p>
    </>
  );
}
